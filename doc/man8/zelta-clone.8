.\" Automatically generated by Pandoc 3.9.0.2
.\"
.TH "zelta\-clone" "8" "" "" "System Manager\(cqs Manual"
.SH NAME
\f[B]zelta clone\f[R] \- perform a recursive clone operation
.SH SYNOPSIS
\f[B]zelta clone\f[R] [\f[I]OPTIONS\f[R]]
\f[I]source\f[R][\(at_snapshot_] \f[I]target\f[R]
.PP
\f[B]zelta clone\f[R] [\f[I]OPTIONS\f[R]] \f[I]source\f[R]
\f[I]target\f[R] \f[I]origin\-backup\f[R] \f[I]target\-backup\f[R]
.SH DESCRIPTION
\f[B]zelta clone\f[R] performs a recursive \f[B]zfs clone\f[R] operation
on a dataset.
This is useful for recursive duplication of dataset trees and backup
inspection and recovery of files replicated with \f[B]zelta backup\f[R].
The clones will reference the latest or indicated snapshot, and consume
practically no additional space.
Clones can be modified and destroyed without affecting their origin
datasets.
.PP
The \f[I]source\f[R] and \f[I]target\f[R] must be on the same host and
pool.
The mountpoint will be inherited below the target parent (as provided by
\f[B]zfs clone\f[R]).
The \f[I]target\f[R] dataset must not exist.
To create a clone on a remote host, ensure the \f[I]source\f[R] and
\f[I]target\f[R] are identical including the username and hostname used.
.PP
With four endpoints, \f[B]zelta clone\f[R] first creates the local
clone, snapshots the new clone, then backs up that clone to
\f[I]target\-backup\f[R] using \f[I]origin\-backup\f[R] as the
replicated origin.
This preserves incremental backup continuity for newly cloned datasets
without sending a full backup.
.PP
Remote endpoint names follow \f[B]scp(1)\f[R] conventions.
Dataset names follow \f[B]zfs(8)\f[R] naming conventions.
.PP
Example remote operation:
.IP
.EX
zelta clone backup\(athost1.com:tank/zones/data backup\(athost1.com:tank/clones/data
.EE
.SH OPTIONS
\f[B]Endpoint Arguments (Required)\f[R]
.TP
\f[I]source\f[R]
A dataset, in the form \f[B]pool[/dataset][\(atsnapshot]\f[R], which
will be cloned along with all of its descendants.
If a snapshot is not given, the most recent snapshot will be used.
.TP
\f[I]target\f[R]
A dataset on the same pool as the \f[I]source\f[R], where the clones
will be created.
This dataset must not exist.
.TP
\f[I]origin\-backup\f[R]
The backup\-side dataset corresponding to \f[I]source\f[R].
Used only with the four\-endpoint form.
.TP
\f[I]target\-backup\f[R]
The backup destination for \f[I]target\f[R] which will be created.
Used only with the four\-endpoint form.
.PP
\f[B]Output Options\f[R]
.TP
\f[B]\-v, \-\-verbose\f[R]
Increase verbosity.
Specify once for operational detail, twice (\f[CR]\-vv\f[R]) for debug
output.
.TP
\f[B]\-q, \-\-quiet\f[R]
Decrease log level.
Specify once to suppress notices, twice (\f[CR]\-qq\f[R]) to suppress
warnings.
.TP
\f[B]\-n, \-\-dryrun, \-\-dry\-run\f[R]
Display \f[CR]zfs\f[R] commands without executing them.
.PP
\f[B]Snapshot Options\f[R]
.TP
\f[B]\-\-snapshot, \-\-snapshot\-always\f[R]
Ensure a snapshot before cloning.
.TP
\f[B]\-\-snap\-name\f[R] \f[I]NAME\f[R]
Specify snapshot name.
Use \f[CR]$(command)\f[R] for dynamic generation.
Default: \f[CR]$(date \-u +zelta_%Y\-%m\-%d_%H.%M.%S)\f[R].
.TP
\f[B]\-\-snap\-prefix\f[R] \f[I]PREFIX\f[R]
Prefix generated snapshot names.
If the resolved name starts with \f[CR]zelta\f[R], \f[I]PREFIX\f[R]
replaces \f[CR]zelta\f[R]; otherwise \f[I]PREFIX\f[R] is prepended
verbatim.
.PP
\f[B]Dataset Options\f[R]
.TP
\f[B]\-d, \-\-depth\f[R] \f[I]LEVELS\f[R]
Limit recursion depth.
For example, a depth of 1 includes only the specified dataset.
.SH EXAMPLES
Clone a dataset tree:
.IP
.EX
zelta clone tank/vm/myos tank/temp/myos\-202404
.EE
.PP
Clone a dataset tree and add a backup for the clone using the existing
origin backup:
.IP
.EX
zelta clone sink/source sink/target tank/source tank/target
.EE
.PP
Recover a dataset tree, in place, to a previous snapshot\(cqs state:
.IP
.EX
zfs rename tank/vm/myos tank/Archives/myos\-202404
zelta clone tank/Archives/myos\-202404\(atgoodsnapshot tank/vm/myos
.EE
.PP
Dry run to display the \f[CR]zfs clone\f[R] commands without executing
them:
.IP
.EX
zelta clone \-n tank/source/dataset tank/target/dataset
.EE
.SH EXIT STATUS
Returns 0 on success, non\-zero on error.
.SH NOTES
See \f[B]zelta\-options(7)\f[R] for environment variables and
\f[CR]zelta.env\f[R] configuration.
.SH SEE ALSO
zelta(8), zelta\-options(7), zelta\-backup(8), zelta\-match(8),
zelta\-policy(8), zelta\-revert(8), zelta\-rotate(8), ssh(1), zfs(8),
zfs\-clone(8), zfs\-promote(8)
.SH AUTHORS
Daniel J. Bell <\f[I]bellhyve\(atzelta.space\f[R]>
.SH WWW
https://zelta.space
