output_dir: tmp
shellspec_name: "060_zelta_clone_spec"

example_list:
  - describe_desc: "Test clone"
    tag: standard:60
    skip_if_list:
      - condition: if 'SANDBOX_ZELTA_SRC_DS undefined' test -z "$SANDBOX_ZELTA_SRC_DS"

    test_list:
      - test_name: clone_sub2
        it_desc: zelta clone sub2 (ignore warnings) - %{when_command}
        allow_no_output: true
        when_command: run zelta clone -qq "$SANDBOX_ZELTA_SRC_EP/sub2" "$SANDBOX_ZELTA_SRC_EP/copy_of_sub2"

      - test_name: zfs_list_for_clone
        setup_scripts:
          - "test/test_helper.sh"
        it_desc: verifies the clone - %{when_command}
        when_command: call src_exec zfs list -ro name,origin $SANDBOX_ZELTA_SRC_DS/copy_of_sub2
